# Your existing file content with the following modifications:

# Modified line 99
if: steps.ggshield.outcome == 'failure' && env.NOTIFICATION_EMAIL != ''

# Added NOTIFICATION_EMAIL to the env section
        NOTIFICATION_EMAIL: ${{ secrets.NOTIFICATION_EMAIL }}

# Modified line 217
if: steps.ggshield_bfde.outcome == 'failure' && env.NOTIFICATION_EMAIL != ''

# Added NOTIFICATION_EMAIL to the env section again
        NOTIFICATION_EMAIL: ${{ secrets.NOTIFICATION_EMAIL }}
